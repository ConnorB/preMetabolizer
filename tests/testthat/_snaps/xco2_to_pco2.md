# xCO2_to_pCO2() is deprecated

    Code
      invisible(xCO2_to_pCO2(420, 20, 1, "atm"))
    Condition
      Warning:
      `xCO2_to_pCO2()` was deprecated in preMetabolizer 0.0.0.9000.
      i Please use `xco2_to_pco2()` instead.

# pCO2_to_xCO2() is deprecated

    Code
      invisible(pCO2_to_xCO2(20, 400, 1, "atm"))
    Condition
      Warning:
      `pCO2_to_xCO2()` was deprecated in preMetabolizer 0.0.0.9000.
      i Please use `pco2_to_xco2()` instead.

