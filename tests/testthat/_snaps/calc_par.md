# calc_par errors on a coordinate length that is neither 1 nor n

    Code
      calc_par(noon, latitude = c(39.1, 40, 41), longitude = -96.6)
    Condition
      Error in `calc_par()`:
      ! `latitude` must be length 1 or 2, not length 3.
