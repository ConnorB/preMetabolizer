% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghcnh.R
\name{get_ghcnh}
\alias{get_ghcnh}
\title{Get GHCNh hourly observations}
\usage{
get_ghcnh(stations, start_date, end_date, quiet = FALSE)
}
\arguments{
\item{stations}{Character vector of GHCNh station identifiers (e.g.,
\code{"USW00023183"}). These match the \code{station_id} column returned by
\code{\link[=get_noaa_stations]{get_noaa_stations()}} and \code{\link[=closest_noaa_stations]{closest_noaa_stations()}}.}

\item{start_date, end_date}{Start and end of the requested period as
\code{Date} objects or \code{"YYYY-MM-DD"} strings.}

\item{quiet}{Logical. When \code{TRUE} progress messages are suppressed.
Default \code{FALSE}.}
}
\value{
A \link[tibble:tibble-package]{tibble} with one row per
station-hour. Key columns include:
\describe{
\item{\code{station_id}}{Station identifier.}
\item{\code{station_name}}{Station name (when available).}
\item{\code{datetime}}{Observation time as a UTC \code{POSIXct}.}
\item{\code{temperature}}{Air temperature in degrees Celsius.}
\item{\code{dew_point_temperature}}{Dew-point temperature (°C).}
\item{\code{relative_humidity}}{Relative humidity (\%).}
\item{\code{wind_direction}}{Wind direction (degrees clockwise from
north).}
\item{\code{wind_speed}}{Wind speed (m/s).}
\item{\code{sea_level_pressure}}{Sea-level pressure (hPa).}
\item{\code{precipitation}}{Precipitation (mm).}
}
Columns that are entirely \code{NA} across all stations and hours are
dropped from the result.
}
\description{
Downloads Global Historical Climatology Network hourly (GHCNh) data
for one or more stations and a date range, returning a parsed tibble
ready for use in stream metabolism modelling.
}
\details{
GHCNh v1.1.0 data are served as pipe-separated (PSV) files from
the NOAA NCEI archive, one file per station per year. Data are
current through the present year. The function downloads all
station-year combinations covering the requested date range in
parallel.

Sentinel values of \code{-9999} are converted to \code{NA}. Columns that
are all \code{NA} are removed.

Multiple stations and years are downloaded in parallel using up
to four concurrent connections.
}
\examples{
\dontrun{
# Fetch WY 2025 hourly data for a station near Konza Prairie
konza <- closest_noaa_stations(
  latitude = 39.1068806,
  longitude = -96.6117151,
  dist_km = 50
)
hourly <- get_ghcnh(
  stations = konza$station_id[1],
  start_date = "2024-10-01",
  end_date = "2025-09-30"
)
}

}
\seealso{
\code{\link[=get_noaa_stations]{get_noaa_stations()}}, \code{\link[=closest_noaa_stations]{closest_noaa_stations()}},
\code{\link[=ncei_data]{ncei_data()}}
}
