% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_to_solar_time.R
\name{convert_to_solar_time}
\alias{convert_to_solar_time}
\alias{convert_from_solar_time}
\title{Convert a datetime to local solar time}
\usage{
convert_to_solar_time(dateTime, longitude, type = c("mean", "apparent"))

convert_from_solar_time(
  solar_datetime,
  longitude,
  type = c("mean", "apparent")
)
}
\arguments{
\item{dateTime}{A datetime vector. POSIXct in any time zone is accepted;
the function operates on the underlying instant, so a CDT timestamp
and the matching UTC timestamp produce identical results. Character or
\code{Date} input is coerced with \code{as.POSIXct(., tz = "UTC")}.}

\item{longitude}{Numeric. Site longitude in decimal degrees; western
longitudes are negative.}

\item{type}{One of \code{"mean"} (default) or \code{"apparent"}.}

\item{solar_datetime}{A POSIXct vector of solar-time values. The clock
reading is interpreted as solar time even though the tzone attribute is
UTC; this matches the convention used by \code{streamMetabolizer} and
\code{SunCalcMeeus}.}
}
\value{
A POSIXct vector with class \code{c("solar_date", "POSIXct", "POSIXt")}
(\code{convert_to_solar_time()}) or \code{c("POSIXct", "POSIXt")}
(\code{convert_from_solar_time()}). The tzone attribute is \code{"UTC"} in both
cases; the clock reading carries the meaning.
}
\description{
Converts a datetime to mean or apparent (true) solar time at a given
longitude. Mean solar time uses a constant 15 deg/hour longitude offset.
Apparent solar time additionally applies the equation of time and is
computed via \code{\link[SunCalcMeeus:solar_time]{SunCalcMeeus::solar_time()}}.
}
\details{
These helpers are named \code{convert_to_solar_time()} /
\code{convert_from_solar_time()} to avoid shadowing the original
\code{streamMetabolizer::convert_UTC_to_solartime()} and
\code{streamMetabolizer::convert_solartime_to_UTC()}, which can still be called
directly from \code{streamMetabolizer} if you need their behaviour.

The mean offset is \code{longitude / 15 * 3600} seconds. The apparent path
delegates to \code{\link[SunCalcMeeus:solar_time]{SunCalcMeeus::solar_time()}}, which applies the equation of
time using Meeus's algorithms.

\code{convert_from_solar_time()} inverts the forward conversion by computing
the forward offset \emph{at the input solar instant} and subtracting it. This
is exact for \code{type = "mean"}. For \code{type = "apparent"} it is accurate to
within roughly the daily change in the equation of time (~30 s/day), which
is well below typical sensor sampling intervals.
}
\examples{
utc <- as.POSIXct("2024-06-21 18:00:00", tz = "UTC")

convert_to_solar_time(utc, longitude = -96.6)
convert_to_solar_time(utc, longitude = -96.6, type = "apparent")

# A non-UTC POSIXct gives the same result, since the underlying instant
# is what matters.
local <- as.POSIXct("2024-06-21 13:00:00", tz = "America/Chicago")
convert_to_solar_time(local, longitude = -96.6)

solar <- convert_to_solar_time(utc, longitude = -96.6)
convert_from_solar_time(solar, longitude = -96.6)

}
\seealso{
\code{\link[SunCalcMeeus:solar_time]{SunCalcMeeus::solar_time()}},
\code{streamMetabolizer::convert_UTC_to_solartime()},
\code{streamMetabolizer::convert_solartime_to_UTC()}.
}
