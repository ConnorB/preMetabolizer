% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_bin_width.R
\name{calc_bin_width}
\alias{calc_bin_width}
\title{Calculate histogram bin width}
\usage{
calc_bin_width(x, method = "auto")
}
\arguments{
\item{x}{Numeric vector of observations. Must contain at least two
distinct, finite, non-\code{NA} values.}

\item{method}{Single character string specifying the binning rule. One of:
\describe{
\item{\code{"auto"}}{Minimum of Freedman-Diaconis and Sturges (default).
Tends to work well across a wide range of distributions.}
\item{\code{"sturges"}}{Sturges' rule: \eqn{h = R / (\log_2 n + 1)}.
Assumes approximate normality; tends to undersmooth for large
\eqn{n}.}
\item{\code{"fd"}}{Freedman-Diaconis: \eqn{h = 2 \cdot \mathrm{IQR}(x)
    \cdot n^{-1/3}}. Robust to outliers. Falls back to Sturges when
\eqn{\mathrm{IQR}(x) = 0}.}
\item{\code{"sqrt"}}{Square-root rule: \eqn{h = R / \sqrt{n}}. Simple
heuristic used by some spreadsheet applications.}
\item{\code{"rice"}}{Rice rule: \eqn{h = R / (2 n^{1/3})}. Similar to
Sturges but grows more slowly.}
\item{\code{"scott"}}{Scott's rule: \eqn{h = 3.49 \hat\sigma n^{-1/3}}.
Optimal for normal data in the sense of minimising mean integrated
squared error.}
\item{\code{"doane"}}{Doane's rule. Extends Sturges to account for
skewness; better suited to non-normal distributions. Requires
\eqn{n \ge 3}.}
}}
}
\value{
A single positive numeric value giving the bin width.
}
\description{
Computes a suggested bin width for a histogram using one of several
classical rules. \code{NA} values are silently removed before computation.
}
\details{
In all formulae, \eqn{R} denotes the range (\code{diff(range(x))}) and
\eqn{n} the number of non-\code{NA} observations.

The \code{"fd"} fallback to Sturges when \code{IQR(x) == 0} avoids a zero-width
bin, which can occur with heavily discrete or zero-inflated data.
}
\section{Errors}{

The function aborts with an informative message when:
\itemize{
\item \code{x} is not a numeric vector.
\item \code{method} is not a single character string.
\item \code{x} contains no non-\code{NA} values, or only one.
\item All non-\code{NA} values in \code{x} are identical (zero range).
\item \code{method = "doane"} is requested with fewer than 3 observations
(the skewness standard error is undefined for \eqn{n < 3}).
\item An unrecognised \code{method} string is supplied.
}
}

\examples{
# Continuous data — Freedman-Diaconis
calc_bin_width(rnorm(200), "fd")

# Skewed data — Doane corrects for skewness
calc_bin_width(rexp(200), "doane")

# Discrete / zero-inflated data — FD falls back to Sturges
calc_bin_width(c(rep(0, 50), rep(1, 50)), "fd")

# NA values are silently dropped
calc_bin_width(c(NA, rnorm(100), NA), "scott")

# Compare all methods on the same data
x <- rnorm(500)
methods <- c("auto", "sturges", "fd", "sqrt", "rice", "scott", "doane")
vapply(methods, \(m) calc_bin_width(x, m), numeric(1))

# Error conditions
try(calc_bin_width(letters))               # non-numeric x
try(calc_bin_width(c(1, NA, NA)))          # only one non-NA value
try(calc_bin_width(rep(5, 50)))            # zero range
try(calc_bin_width(rnorm(200), "badrule")) # unknown method
try(calc_bin_width(c(1, 2), "doane"))      # doane needs n >= 3

}
