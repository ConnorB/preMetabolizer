% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdo.R
\name{cdo}
\alias{cdo}
\alias{cdo_datasets}
\alias{cdo_data_categories}
\alias{cdo_data_types}
\alias{cdo_location_categories}
\alias{cdo_locations}
\alias{cdo_stations}
\alias{cdo_data}
\title{Query the NCEI Climate Data Online (CDO) Web Services v2 API}
\usage{
cdo_datasets(
  id = NULL,
  datatype_id = NULL,
  location_id = NULL,
  station_id = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf,
  datatypeid = lifecycle::deprecated(),
  locationid = lifecycle::deprecated(),
  stationid = lifecycle::deprecated(),
  startdate = lifecycle::deprecated(),
  enddate = lifecycle::deprecated(),
  sortfield = lifecycle::deprecated(),
  sortorder = lifecycle::deprecated()
)

cdo_data_categories(
  id = NULL,
  dataset_id = NULL,
  location_id = NULL,
  station_id = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf
)

cdo_data_types(
  id = NULL,
  dataset_id = NULL,
  location_id = NULL,
  station_id = NULL,
  data_category_id = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf
)

cdo_location_categories(
  id = NULL,
  dataset_id = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf
)

cdo_locations(
  id = NULL,
  dataset_id = NULL,
  location_category_id = NULL,
  data_category_id = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf,
  datasetid = lifecycle::deprecated(),
  locationcategoryid = lifecycle::deprecated(),
  datacategoryid = lifecycle::deprecated(),
  startdate = lifecycle::deprecated(),
  enddate = lifecycle::deprecated(),
  sortfield = lifecycle::deprecated(),
  sortorder = lifecycle::deprecated()
)

cdo_stations(
  id = NULL,
  dataset_id = NULL,
  location_id = NULL,
  data_category_id = NULL,
  datatype_id = NULL,
  extent = NULL,
  start_date = NULL,
  end_date = NULL,
  sort_field = NULL,
  sort_order = NULL,
  max_results = Inf,
  datasetid = lifecycle::deprecated(),
  locationid = lifecycle::deprecated(),
  datacategoryid = lifecycle::deprecated(),
  datatypeid = lifecycle::deprecated(),
  startdate = lifecycle::deprecated(),
  enddate = lifecycle::deprecated(),
  sortfield = lifecycle::deprecated(),
  sortorder = lifecycle::deprecated()
)

cdo_data(
  dataset_id = NULL,
  start_date = NULL,
  end_date = NULL,
  datatype_id = NULL,
  location_id = NULL,
  station_id = NULL,
  units = "metric",
  sort_field = NULL,
  sort_order = NULL,
  include_metadata = FALSE,
  max_results = Inf,
  datasetid = lifecycle::deprecated(),
  startdate = lifecycle::deprecated(),
  enddate = lifecycle::deprecated(),
  datatypeid = lifecycle::deprecated(),
  locationid = lifecycle::deprecated(),
  stationid = lifecycle::deprecated(),
  sortfield = lifecycle::deprecated(),
  sortorder = lifecycle::deprecated(),
  includemetadata = lifecycle::deprecated()
)
}
\arguments{
\item{id}{Optional character string. When supplied, fetches a single
resource by its identifier (e.g., \code{cdo_datasets("GHCND")}) and
returns a named list. When \code{NULL} (default) the list endpoint is
queried and a tibble is returned.}

\item{start_date, end_date}{Optional date filters as \code{Date} objects or
\code{"YYYY-MM-DD"} strings. For \code{cdo_data()} both are required.}

\item{sort_field, sort_order}{Optional sort controls. \code{sort_field} is one
of \code{"id"}, \code{"name"}, \code{"mindate"}, \code{"maxdate"}, \code{"datacoverage"};
\code{sort_order} is \code{"asc"} (default) or \code{"desc"}.}

\item{max_results}{Maximum number of rows to return. Defaults to
\code{Inf} (all matching results).}

\item{dataset_id, datatype_id, location_id, station_id, data_category_id, location_category_id}{Optional character vectors filtering the result set. Multiple
values are sent as repeated query parameters.}

\item{datasetid, datatypeid, locationid, stationid, datacategoryid, locationcategoryid, startdate, enddate, sortfield, sortorder, includemetadata}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{dataset_id}, \code{datatype_id},
\code{location_id}, \code{station_id}, \code{data_category_id},
\code{location_category_id}, \code{start_date}, \code{end_date}, \code{sort_field},
\code{sort_order}, and \code{include_metadata} instead.}

\item{extent}{For \code{cdo_stations()} only. Numeric vector of length 4
giving a bounding box as \code{c(min_lat, min_lon, max_lat, max_lon)}.}

\item{units}{For \code{cdo_data()} only. \code{"metric"} (default) or
\code{"standard"}.}

\item{include_metadata}{For \code{cdo_data()} only. Logical; when \code{FALSE}
(default) the API skips computing the result-set count, which can
noticeably speed up large requests.}
}
\value{
A \link[tibble:tibble-package]{tibble} for list queries, or a
named list for single-resource (\code{id}-based) queries. Date-like
columns (\code{mindate}, \code{maxdate}, \code{date}) are parsed to \code{Date} /
\code{POSIXct} and numeric columns (\code{latitude}, \code{longitude},
\code{elevation}, \code{value}, \code{datacoverage}) are coerced to numeric.
}
\description{
These functions wrap the seven endpoints of NOAA's Climate Data Online
Web Services v2 API
(\verb{https://www.ncei.noaa.gov/cdo-web/api/v2}). They retrieve
observations (\code{cdo_data()}), search the station catalog
(\code{cdo_stations()}), and browse the metadata catalog
(\code{cdo_datasets()}, \code{cdo_data_categories()}, \code{cdo_data_types()},
\code{cdo_location_categories()}, \code{cdo_locations()}).
}
\section{Authentication}{

All CDO endpoints require a free API token. Request one at
\url{https://www.ncdc.noaa.gov/cdo-web/token} and store it in the
\code{API_NCEI_CDO} environment variable, for example by adding
\code{API_NCEI_CDO=...} to your \verb{~/.Renviron}.
}

\section{Pagination}{

List endpoints paginate at 1000 results per page. By default each
function transparently walks all pages and returns a single tibble.
Use \code{max_results} to cap the total number of rows returned.
}

\section{Rate limits}{

Each token is limited to 5 requests per second and 10,000 requests
per day. The 5/s cap is enforced client-side via
\code{\link[httr2:req_throttle]{httr2::req_throttle()}} (requests block briefly rather than failing).
The package also tracks a session-local counter; once 10,000
successful requests have been made in the current R session the next
call aborts with an error directing you to
\code{\link[=cdo_reset_request_count]{cdo_reset_request_count()}}. The counter is best-effort and does not
include requests made by other R sessions or tools.
}

\examples{
\dontrun{
cdo_datasets()
cdo_datasets("GHCND")
}
\dontrun{
cdo_data_categories(dataset_id = "GHCND")
}
\dontrun{
cdo_data_types(dataset_id = "GHCND", data_category_id = "TEMP")
cdo_data_types("TMAX")
}
\dontrun{
cdo_location_categories()
cdo_location_categories("ST")
}
\dontrun{
cdo_locations(location_category_id = "ST")
cdo_locations("FIPS:37")
}
\dontrun{
cdo_stations(location_id = "FIPS:37", dataset_id = "GHCND")
cdo_stations("GHCND:USW00013722")
}
\dontrun{
cdo_data(
  dataset_id = "GHCND",
  station_id = "GHCND:USW00013722",
  start_date = "2024-01-01",
  end_date   = "2024-01-31",
  datatype_id = c("TMAX", "TMIN")
)
}
}
\seealso{
\code{\link[=ncei_data]{ncei_data()}} and \code{\link[=ncei_stations]{ncei_stations()}} for the parallel NCEI
Access Data Service (no token required, different endpoint
structure).
}
