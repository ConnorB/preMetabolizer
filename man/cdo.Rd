% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdo.R
\name{cdo}
\alias{cdo}
\alias{cdo_datasets}
\alias{cdo_datacategories}
\alias{cdo_datatypes}
\alias{cdo_locationcategories}
\alias{cdo_locations}
\alias{cdo_stations}
\alias{cdo_data}
\title{Query the NCEI Climate Data Online (CDO) Web Services v2 API}
\usage{
cdo_datasets(
  id = NULL,
  datatypeid = NULL,
  locationid = NULL,
  stationid = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_datacategories(
  id = NULL,
  datasetid = NULL,
  locationid = NULL,
  stationid = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_datatypes(
  id = NULL,
  datasetid = NULL,
  locationid = NULL,
  stationid = NULL,
  datacategoryid = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_locationcategories(
  id = NULL,
  datasetid = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_locations(
  id = NULL,
  datasetid = NULL,
  locationcategoryid = NULL,
  datacategoryid = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_stations(
  id = NULL,
  datasetid = NULL,
  locationid = NULL,
  datacategoryid = NULL,
  datatypeid = NULL,
  extent = NULL,
  startdate = NULL,
  enddate = NULL,
  sortfield = NULL,
  sortorder = NULL,
  max_results = Inf
)

cdo_data(
  datasetid,
  startdate,
  enddate,
  datatypeid = NULL,
  locationid = NULL,
  stationid = NULL,
  units = "metric",
  sortfield = NULL,
  sortorder = NULL,
  includemetadata = FALSE,
  max_results = Inf
)
}
\arguments{
\item{id}{Optional character string. When supplied, fetches a single
resource by its identifier (e.g., \code{cdo_datasets("GHCND")}) and
returns a named list. When \code{NULL} (default) the list endpoint is
queried and a tibble is returned.}

\item{startdate, enddate}{Optional date filters as \code{Date} objects or
\code{"YYYY-MM-DD"} strings. For \code{cdo_data()} both are required.}

\item{sortfield, sortorder}{Optional sort controls. \code{sortfield} is one
of \code{"id"}, \code{"name"}, \code{"mindate"}, \code{"maxdate"}, \code{"datacoverage"};
\code{sortorder} is \code{"asc"} (default) or \code{"desc"}.}

\item{max_results}{Maximum number of rows to return. Defaults to
\code{Inf} (all matching results).}

\item{datasetid, datatypeid, locationid, stationid, datacategoryid, locationcategoryid}{Optional character vectors filtering the result set. Multiple
values are sent as repeated query parameters.}

\item{extent}{For \code{cdo_stations()} only. Numeric vector of length 4
giving a bounding box as \code{c(min_lat, min_lon, max_lat, max_lon)}.}

\item{units}{For \code{cdo_data()} only. \code{"metric"} (default) or
\code{"standard"}.}

\item{includemetadata}{For \code{cdo_data()} only. Logical; when \code{FALSE}
(default) the API skips computing the result-set count, which can
noticeably speed up large requests.}
}
\value{
A \link[tibble:tibble-package]{tibble} for list queries, or a
named list for single-resource (\code{id}-based) queries. Date-like
columns (\code{mindate}, \code{maxdate}, \code{date}) are parsed to \code{Date} /
\code{POSIXct} and numeric columns (\code{latitude}, \code{longitude},
\code{elevation}, \code{value}, \code{datacoverage}) are coerced to numeric.
}
\description{
These functions wrap the seven endpoints of NOAA's Climate Data Online
Web Services v2 API
(\verb{https://www.ncei.noaa.gov/cdo-web/api/v2}). They retrieve
observations (\code{cdo_data()}), search the station catalog
(\code{cdo_stations()}), and browse the metadata catalog
(\code{cdo_datasets()}, \code{cdo_datacategories()}, \code{cdo_datatypes()},
\code{cdo_locationcategories()}, \code{cdo_locations()}).
}
\section{Authentication}{

All CDO endpoints require a free API token. Request one at
\url{https://www.ncdc.noaa.gov/cdo-web/token} and store it in the
\code{API_NCEI_CDO} environment variable, for example by adding
\code{API_NCEI_CDO=...} to your \verb{~/.Renviron}.
}

\section{Pagination}{

List endpoints paginate at 1000 results per page. By default each
function transparently walks all pages and returns a single tibble.
Use \code{max_results} to cap the total number of rows returned.
}

\section{Rate limits}{

Each token is limited to 5 requests per second and 10,000 requests
per day. The 5/s cap is enforced client-side via
\code{\link[httr2:req_throttle]{httr2::req_throttle()}} (requests block briefly rather than failing).
The package also tracks a session-local counter; once 10,000
successful requests have been made in the current R session the next
call aborts with an error directing you to
\code{\link[=cdo_reset_request_count]{cdo_reset_request_count()}}. The counter is best-effort and does not
include requests made by other R sessions or tools.
}

\examples{
\dontrun{
cdo_datasets()
cdo_datasets("GHCND")
}
\dontrun{
cdo_datacategories(datasetid = "GHCND")
}
\dontrun{
cdo_datatypes(datasetid = "GHCND", datacategoryid = "TEMP")
cdo_datatypes("TMAX")
}
\dontrun{
cdo_locationcategories()
cdo_locationcategories("ST")
}
\dontrun{
cdo_locations(locationcategoryid = "ST")
cdo_locations("FIPS:37")
}
\dontrun{
cdo_stations(locationid = "FIPS:37", datasetid = "GHCND")
cdo_stations("GHCND:USW00013722")
}
\dontrun{
cdo_data(
  datasetid = "GHCND",
  stationid = "GHCND:USW00013722",
  startdate = "2024-01-01",
  enddate   = "2024-01-31",
  datatypeid = c("TMAX", "TMIN")
)
}
}
\seealso{
\code{\link[=ncei_data]{ncei_data()}} and \code{\link[=ncei_stations]{ncei_stations()}} for the parallel NCEI
Access Data Service (no token required, different endpoint
structure).
}
