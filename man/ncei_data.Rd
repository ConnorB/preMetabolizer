% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncei_data.R
\name{ncei_data}
\alias{ncei_data}
\title{Get data from the NCEI Data Service API}
\usage{
ncei_data(
  dataset,
  stations,
  start_date,
  end_date,
  data_types = NULL,
  units = "metric",
  include_station_name = TRUE,
  include_station_location = FALSE
)
}
\arguments{
\item{dataset}{Character string. The dataset to query. Common values
include \code{"daily-summaries"} (GHCND), \code{"global-hourly"} (ISD),
\code{"global-summary-of-the-month"}, and \code{"global-summary-of-the-year"}.
New datasets are added periodically; use \code{\link[=ncei_datasets]{ncei_datasets()}} to list
what is currently available.}

\item{stations}{Character vector of station identifiers, such as those
returned by \code{\link[=ncei_stations]{ncei_stations()}} or \code{\link[=get_noaa_stations]{get_noaa_stations()}}.}

\item{start_date, end_date}{Start and end of the requested period, as
\code{Date} objects or \code{"YYYY-MM-DD"} strings.}

\item{data_types}{Optional character vector of data type codes to
include (e.g., \code{c("TMAX", "TMIN", "PRCP")} for daily summaries).
When \code{NULL} all available types are returned.}

\item{units}{Character string. \code{"metric"} (default) or \code{"standard"}.}

\item{include_station_name}{Logical. When \code{TRUE} (default), a \code{NAME}
column is included in the result.}

\item{include_station_location}{Logical. When \code{TRUE}, \code{LATITUDE},
\code{LONGITUDE}, and \code{ELEVATION} columns are added. Default \code{FALSE}.}
}
\value{
A data frame with one row per observation. The \code{STATION} column
identifies the source station and \code{DATE} records the observation
time. Additional columns depend on the requested \code{dataset} and
\code{data_types}.
}
\description{
Retrieves weather and climate data from NOAA's National Centers for
Environmental Information (NCEI) using the Access Data Service API.
}
\details{
This function calls
\verb{https://www.ncei.noaa.gov/access/services/data/v1}. Data are returned
in CSV format and read into a data frame with \code{\link[readr:read_csv]{readr::read_csv()}}.

The \code{"daily-summaries"} dataset returns one row per station per day
with columns such as \code{PRCP}, \code{TMAX}, and \code{TMIN}. The \code{"global-hourly"}
dataset returns ISD records in which several key variables (\code{TMP},
\code{WND}, \code{DEW}, \code{SLP}) contain comma-separated sub-fields rather than
simple numeric values.
}
\examples{
\dontrun{
# Daily temperature and precipitation at a single station
ncei_data(
  dataset = "daily-summaries",
  stations = "USW00023183",
  start_date = "2023-01-01",
  end_date = "2023-12-31",
  data_types = c("TMAX", "TMIN", "PRCP")
)

# Hourly ISD observations
ncei_data(
  dataset = "global-hourly",
  stations = "72469023183",
  start_date = "2023-06-01",
  end_date = "2023-06-30"
)
}

}
\seealso{
\code{\link[=ncei_stations]{ncei_stations()}} to search for station identifiers,
\code{\link[=ncei_datasets]{ncei_datasets()}} to list available datasets.
}
