% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncei_stations.R
\name{ncei_stations}
\alias{ncei_stations}
\title{Search for NCEI weather stations}
\usage{
ncei_stations(
  dataset,
  bbox = NULL,
  start_date = NULL,
  end_date = NULL,
  data_types = NULL,
  text = NULL,
  limit = 100L,
  offset = 0L
)
}
\arguments{
\item{dataset}{Character string. The dataset to search within, such as
\code{"daily-summaries"} (GHCND) or \code{"global-hourly"} (ISD). See
\code{\link[=ncei_datasets]{ncei_datasets()}} for more dataset identifiers.}

\item{bbox}{Optional numeric vector of length 4 specifying the
geographic search area as \code{c(north, west, south, east)} in decimal
degrees. North and south must be between -90 and 90; west and east
between -180 and 180. When \code{NULL} no geographic filter is applied.}

\item{start_date, end_date}{Optional date range filter. Only stations
with data overlapping this period are returned. Accepts \code{Date}
objects or \code{"YYYY-MM-DD"} strings.}

\item{data_types}{Optional character vector of data type codes. Only
stations that include all requested types are returned.}

\item{text}{Optional character string. Filters results to stations
whose names contain this text.}

\item{limit}{Integer. Maximum number of stations to return per page
(default 100, max 1000).}

\item{offset}{Integer. Zero-based pagination offset (default 0).}
}
\value{
A \link[tibble:tibble-package]{tibble} with one row per station and
the following columns:
\describe{
\item{\code{station_id}}{Station identifier (dataset prefix stripped,
e.g., \code{"USW00023183"} rather than \code{"GHCND:USW00023183"}).}
\item{\code{name}}{Station name.}
\item{\code{latitude},\code{longitude}}{Decimal-degree coordinates.}
\item{\code{elevation}}{Elevation in metres.}
\item{\code{start_date},\code{end_date}}{Period of record as \code{Date} objects.}
\item{\code{data_coverage}}{Fraction of expected observations present
(0–1).}
}
}
\description{
Searches for stations in NOAA's National Centers for Environmental
Information (NCEI) using the Common Access Search Service API.
}
\details{
This function calls
\verb{https://www.ncei.noaa.gov/access/services/search/v1/data}.

To find stations near a specific point, compute a bounding box with
\code{\link[=ncei_bbox]{ncei_bbox()}} and pass it to \code{bbox}. For site-level station searches
prefer \code{\link[=closest_noaa_stations]{closest_noaa_stations()}}, which computes the bounding box
automatically and filters by geodesic distance.
}
\examples{
\dontrun{
# All daily-summaries stations in a region
ncei_stations(
  dataset = "daily-summaries",
  bbox = c(40, -100, 38, -98)
)

# Hourly stations with temperature data and a long record
ncei_stations(
  dataset = "global-hourly",
  start_date = "1990-01-01",
  end_date = Sys.Date()
)
}

}
\seealso{
\code{\link[=closest_noaa_stations]{closest_noaa_stations()}}, \code{\link[=get_noaa_stations]{get_noaa_stations()}},
\code{\link[=ncei_bbox]{ncei_bbox()}}, \code{\link[=ncei_data]{ncei_data()}}
}
