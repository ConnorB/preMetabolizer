% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_mode.R
\name{calc_mode}
\alias{calc_mode}
\title{Calculate the Mode(s) of a Vector}
\usage{
calc_mode(x, na.rm = TRUE)
}
\arguments{
\item{x}{A vector (numeric, character, factor, etc.) for which to compute the mode(s).}

\item{na.rm}{Logical indicating whether to remove NA values before computation (default: TRUE).}
}
\value{
A vector of the same type as \code{x} containing the mode(s). Returns NA if:
\itemize{
\item Input is empty
\item Input contains only NAs (when na.rm = TRUE)
\item No mode can be determined
}
}
\description{
Computes the mode (most frequent value) of a vector. Handles multiple modes, NA values,
and preserves the input data type. For empty inputs or all-NA inputs, returns NA.
}
\examples{
# Single mode
calc_mode(c(1, 2, 2, 3, 3, 3))  # 3

# Multiple modes
calc_mode(c(1, 1, 2, 2, 3))     # c(1, 2)

# Character vector
calc_mode(c("a", "b", "b", "c")) # "b"

# With NA values
calc_mode(c(1, 2, 2, NA))        # 2
calc_mode(c(NA, NA), na.rm = FALSE) # NA

# Edge cases
calc_mode(integer(0))            # NA
calc_mode(c(NA, NA))             # NA

}
